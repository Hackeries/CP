#include<bits/stdc++.h>
using namespace std;
void solve(){
    int n,q;
    cin >> n >> q;

    vector<int>vn(n);
    for(int i=0;i<n;++i){
        cin >> vn[i];
    }

    vector<int>vq(q);
    for(int i=0;i<q;++i){
        int xi,yi;
        cin >> xi >> yi;

        
    }
}
int main(){
    ios_base::sync_with_stdio(false);
	cin.tie(0);

    
    solve();
}